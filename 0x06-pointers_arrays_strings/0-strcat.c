#include "main.h"
#include <string>

/**
 * _strcat - concanete string
 * @dest: string 1
 * @src: string 2
 *
 * Retrun: string
 */
char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return (dest);
}
